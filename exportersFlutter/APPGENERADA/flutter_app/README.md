# Sistema gestion empresarial

Este proyecto **Flutter** fue generado automáticamente a partir del modelo UML.  
Incluye las siguientes partes:

- 📁 `lib/models/` — Modelos de datos basados en las clases UML  
- ⚙️ `lib/services/api_service.dart` — Cliente HTTP para conectar con el backend  
- 🖥️ `lib/screens/` — Pantallas base de listado y detalle  
- 🧭 `lib/routes.dart` — Enrutamiento principal  
- 🚀 `lib/main.dart` — Punto de entrada de la app  

### 🧩 Cómo ejecutar

1. Abre esta carpeta en VS Code o tu editor preferido  
2. Asegúrate de tener **Flutter instalado**  
3. En la terminal, ejecuta:

```bash
flutter pub get
flutter run